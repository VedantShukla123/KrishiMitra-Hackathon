# Copy this file to .env and fill in real values. Never commit .env to git.
# backend/.env.example

# --- You GENERATE these yourself (long random strings). See README or below. ---
SECRET_KEY=generate-a-long-random-string-for-flask-sessions
JWT_SECRET_KEY=generate-another-long-random-string-for-jwt-tokens

# --- Database (default is fine for local dev) ---
DATABASE_URL=sqlite:///krishimitra.db

# --- Get from Google AI Studio: https://aistudio.google.com/app/apikey ---
GEMINI_API_KEY=your-gemini-api-key-from-google-ai-studio
GEMINI_MODEL=gemini-pro

# --- Optional: only if you use OpenAI for crop analysis fallback ---
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_MODEL=gpt-4o-mini
